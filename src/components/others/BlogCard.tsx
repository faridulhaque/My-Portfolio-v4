import React from 'react';

const BlogCard = () => {
    return (
        <div className="group w-full h-[500px] overflow-hidden relative">
            
        </div>
    );
};

export default BlogCard;